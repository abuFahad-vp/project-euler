
#include <stdbool.h>
#define ll long long

typedef struct 
{
  int length;
  int period[200];
} contFrac;

//Generate primes from lower to upper 
//and return total number of primes
int Period(contFrac []);

ll Primes(ll [],int,int);

extern int matrix[6400][6400];

bool solveSudoku(int [9][9]);
