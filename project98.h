#include <vector>
#include <string>
#include <iostream>
#include <map>
#include <algorithm>
#include <fstream>
#include <set>

std::vector<std::string> readWords();
std::vector<std::vector<std::string>> readAnagram();